# library-management-sql
A relational database system for library management built with MySQL. Includes schema design with normalized tables, foreign key constraints, and analytical SQL queries for tracking book issues, returns, overdue fines, and borrowing trends.
